import fs from 'node:fs';

await import('./build-v18.mjs');
let html=fs.readFileSync('index.html','utf8');
const methods=fs.readFileSync('v19/rectify-count.fragment','utf8').trimEnd();
const excelTables=fs.readFileSync('v19/excel-tables.fragment','utf8').trimEnd();
const mustReplace=(from,to,label)=>{if(!html.includes(from))throw new Error(`NOVA V19 build contract missing: ${label}`);html=html.replace(from,to)};
const mustRegex=(re,to,label)=>{if(!re.test(html))throw new Error(`NOVA V19 build contract missing: ${label}`);html=html.replace(re,to)};

mustReplace('<title>Magazzino NOVA · Teramo · V18</title>','<title>Magazzino NOVA · Teramo · V19</title>','V19 title');
mustReplace('</head>','<link rel="stylesheet" href="v19/ux-v19.css?v=20260901-v19"></head>','V19 UX stylesheet');

/* Persistent export sequence and persisted assisted-count draft. */
mustReplace("settings:{lastExportAt:null,lastExportName:'',legacyMigratedAt:null}","settings:{lastExportAt:null,lastExportName:'',legacyMigratedAt:null,exportVersion:0,countDraft:null}",'V19 settings');

/* Master aliases include operational note and last inventory/control date. */
mustReplace("pallet:['BANCALE','PALLET','BANCALE CARRELLO','CARRELLO'],state:['STATO','STATO MERCE']","pallet:['BANCALE','PALLET','BANCALE CARRELLO','CARRELLO'],note:['NOTE','NOTA','ANNOTAZIONI'],checkedAt:['DATA CONTROLLO QUANTITA','DATA CONTROLLO QUANTITÀ','DATA CONTROLLO','DATA INVENTARIO','ULTIMO CONTROLLO'],state:['STATO','STATO MERCE']",'Master check metadata aliases');

/* Correct cell type for real Excel dates. */
mustReplace("function extendRef(ws,maxRow,maxCol){",`function setDateCell(ws,r,c,value,template){if(c==null||c<0||!value)return;const d=value instanceof Date?value:new Date(value);if(Number.isNaN(d.getTime()))return;const addr=X().utils.encode_cell({r,c}),old=ws[addr]||{},cell={...old,v:d,t:'d',z:'dd/mm/yyyy hh:mm'};delete cell.f;delete cell.F;delete cell.D;if(!old.s&&template?.s)cell.s=clone(template.s);ws[addr]=cell}\nfunction extendRef(ws,maxRow,maxCol){`,'Excel date cell helper');

/* Rectification is traceable even with zero delta and can address a pallet-only bucket. */
mustRegex(/  setQuantity\(\{currentKey='',article,size,state,location,pallet,counted,note=''\}\)\{[^\n]*\}/,`  setQuantity({currentKey='',article,size,state,location,pallet,counted,note='',partials=[],method='RECTIFICATION_MANUAL'}){if(!this.auth.can('RECTIFY'))throw new Error('Operazione non autorizzata.');const current=currentKey?this.stock.findByKey(currentKey):null,item=current?cleanItem(current):cleanItem({article,size,state}),bucket=current?cleanBucket(current):cleanBucket({location,pallet}),old=current?.quantity||0,target=integer(counted),delta=target-old,checkedAt=nowIso(),cleanPartials=(partials||[]).map(integer).filter(x=>x>0);if(!item.article)throw new Error('Inserisci l’articolo.');if(!bucket.location&&!bucket.pallet)throw new Error('Inserisci almeno Fila/Scaffale oppure Bancale.');const source={kind:'RECTIFICATION',method,note:String(note||''),partials:cleanPartials,checkedAt},e=this.ledger.adjust(item,bucket,delta,source);this.audit.record('RECTIFICATION','STOCK',currentKey||bucketKey({...item,...bucket}),{quantity:old},{quantity:target},{note:String(note||''),partials:cleanPartials,method,checkedAt,article:item.article,size:item.size,state:item.state,location:bucket.location,pallet:bucket.pallet});this.store.save('stock:rectify');return e}`,'Rectification trace contract');

/* V19 WMS rectification and assisted-count workflow. */
mustRegex(/  rectify\(key=''\)\{[\s\S]*?\n  finalizeCount\(\)\{[^\n]*\}/,methods,'Rectification/count method block');

/* Live rectify filtering, direct-count metrics and persistent extra fields. */
mustReplace("if(['stockQuery','stockLocation','stockPallet'].includes(el.id))this.renderStockResults();",`if(['stockQuery','stockLocation','stockPallet'].includes(el.id))this.renderStockResults();if(['rectArticle','rectSize','rectLocation','rectPallet'].includes(el.id)){if(el.id==='rectArticle'||el.id==='rectSize')el.value=el.value.toUpperCase();this.renderRectifyResults()}if(el.id==='rectCounted')this.rectDirect(el.value);`,'V19 rectify live input');
mustReplace("if(el.id==='stockState')this.renderStockResults();","if(el.id==='stockState')this.renderStockResults();if(el.id==='rectState')this.renderRectifyResults();",'V19 rectify state input');

/* Route rectification through context-aware opener. */
mustReplace("'rectify-new':()=>go('rectify',{key:''}),rectify:()=>go('rectify',{key:''})","'rectify-new':()=>this.openRectify(''),rectify:()=>this.openRectify('')",'Context-aware rectify route');
mustReplace("if(action==='rectify-open')return go('rectify',{key:el.dataset.key});","if(action==='rectify-open')return this.openRectify(el.dataset.key);if(action==='rectify-pick')return this.rectPick(el.dataset.key);if(action==='rect-partial-add')return this.addRectPartial();if(action==='rect-partial-remove')return this.removeRectPartial(el.dataset.index);if(action==='rect-partial-undo')return this.undoRectPartial();if(action==='rect-partial-clear')return this.clearRectPartials();",'Rectification actions');
mustReplace("if(action==='count-partial-add')return this.addCountPartial(el.dataset.dom);","if(action==='count-partial-add')return this.addCountPartial(el.dataset.dom);if(action==='count-partial-remove')return this.removeCountPartial(el.dataset.dom,el.dataset.index);if(action==='count-confirm')return this.confirmCountRow(el.dataset.dom);if(action==='count-edit')return this.editCountRow(el.dataset.dom);if(action==='count-extra-confirm')return this.confirmCountExtra(el.dataset.dom);if(action==='count-extra-edit')return this.editCountExtra(el.dataset.dom);",'Count confirmation actions');

/* Reimport keeps export lineage even if the visible filename is the only available source. */
mustReplace("next.settings={...(next.settings||{}),lastExportAt:null,lastExportName:''};",`const importedExportVersion=Number((String(file.name||'').match(/GESTIONE MAGAZZINO_v(\\d+)/i)||[])[1]||0);next.settings={...(next.settings||{}),exportVersion:Math.max(Number(next.settings?.exportVersion)||0,importedExportVersion),lastExportAt:null,lastExportName:file.name,countDraft:null};`,'Import export lineage');

/* Professional progressive export filename and rollback-safe sequencing. */
mustRegex(/  async export\(\)\{[\s\S]*?\n  \}\n  async exportRegistry/,`  async export(){\n    await ensureXlsx();\n    const bytes=await idbGet(MASTER_KEY).catch(()=>null),wb=bytes?X().read(bytes,{type:'array',cellDates:true,cellStyles:true,bookVBA:true}):X().utils.book_new(),meta=this.store.db.master,settings=this.store.db.settings||{},exportedAt=nowIso(),previousMetaVersion=Number(meta.version)||0,previousExportVersion=Number(settings.exportVersion)||0,filenameVersion=Number((String(meta.filename||settings.lastExportName||'').match(/GESTIONE MAGAZZINO_v(\\d+)/i)||[])[1]||0),nextExportVersion=Math.max(previousExportVersion,filenameVersion)+1,name=\`GESTIONE MAGAZZINO_v\${nextExportVersion}.xlsx\`;\n    if(!meta.masterId)meta.masterId=masterIdFor(meta.filename,meta.importedAt||exportedAt);meta.version=Math.max(1,previousMetaVersion||1)+1;meta.versionAt=exportedAt;settings.exportVersion=nextExportVersion;settings.lastExportName=name;\n    try{this.updateMagazine(wb);this.replaceSheet(wb,'REGISTRO_MOVIMENTI',this.movementSheet());this.replaceSheet(wb,'ENTRATE_MERCI',this.receiptSheet());this.replaceSheet(wb,'RICHIESTE',this.requestSheet());this.replaceSheet(wb,'SCARICHI',this.issueSheet());this.replaceSheet(wb,'AUDIT',this.auditSheet());this.buildDataSheet(wb);const rawOut=X().write(wb,{type:'array',bookType:'xlsx',compression:true,cellStyles:true,bookSST:false}),out=await enhanceWorkbookTables(rawOut,{masterSheet:this.store.db.master.sheetName||'MAGAZZINO',masterHeaderRow:Number(this.store.db.master.headerRow||0)+1});this.#download(out,name)}catch(error){meta.version=previousMetaVersion;settings.exportVersion=previousExportVersion;throw error}\n    const exportedAudit=this.domain.audit.record('MASTER_EXPORTED','MASTER',name,null,{name,masterId:meta.masterId,version:meta.version,exportVersion:nextExportVersion});settings.lastExportAt=exportedAudit.at;settings.lastExportName=name;this.store.save('master:export:baseline');return name;\n  }\n  async exportRegistry`,'Progressive V19 export');

/* Master projection now propagates latest physical-control date and a non-destructive NOVA control note. */
mustRegex(/  updateMagazine\(wb\)\{[\s\S]*?\n  \}\n  #writeIdentityRow/,`  #latestCheckMap(){const map=new Map();for(const a of this.store.db.audits||[]){if(a.action!=='RECTIFICATION')continue;const p=String(a.entityId||'').split('|'),m=a.meta||{},article=normalizeArticle(m.article||p[0]),size=norm(m.size||p[1]),location=norm(m.location||p[3]),pallet=norm(m.pallet||p[4]);if(!article)continue;const key=[article,size,location,pallet].join('|'),prev=map.get(key);if(!prev||new Date(a.at)>new Date(prev.at))map.set(key,{at:m.checkedAt||a.at,note:String(m.note||''),partials:Array.isArray(m.partials)?m.partials:[],method:m.method||'RECTIFICATION_MANUAL'})}return map}\n  #applyCheckMeta(ws,rowIndex,cols,r,templateRow,checks){const check=checks.get(identity(r));if(!check)return;const template=c=>Number.isInteger(c)&&c>=0?ws[X().utils.encode_cell({r:templateRow,c})]:null;if(cols.checkedAt>=0)setDateCell(ws,rowIndex,cols.checkedAt,check.at,template(cols.checkedAt));if(cols.note>=0){const addr=X().utils.encode_cell({r:rowIndex,c:cols.note}),existing=String(ws[addr]?.v||'').replace(/\\s*\\|?\\s*NOVA CONTROLLO:.*$/i,'').trim(),label=check.method==='ASSISTED_COUNT'?'Conteggio assistito':'Rettifica manuale',parts=check.partials.length?\` · \${check.partials.join(' + ')}\`:'',userNote=check.note&&!/^Conteggio assistito/i.test(check.note)?\` · \${check.note}\`:'',auto=\`NOVA CONTROLLO: \${label}\${parts}\${userNote}\`,value=existing?\`\${existing} | \${auto}\`:auto;setCell(ws,rowIndex,cols.note,value,template(cols.note))}}\n  updateMagazine(wb){\n    const meta=this.store.db.master,current=this.domain.stock.positive(),sheetName=meta.sheetName||'MAGAZZINO';let ws=wb.Sheets[sheetName];if(!ws||!meta.columns||meta.columns.article==null){this.#replaceSimpleMagazine(wb,sheetName,current);return}\n    const cols=meta.columns,mode=meta.mode||'wide',sourceRows=normalizeMasterRows(meta.excelRows?.length?meta.excelRows:meta.rows),maxCol=Math.max(...Object.values(cols).filter(x=>Number.isInteger(x)&&x>=0),0),templateRow=Math.max(meta.headerRow+1,sourceRows.find(r=>r.sourceRow!=null)?.sourceRow??meta.headerRow+1),checks=this.#latestCheckMap();\n    if(mode==='wide'){const aggregate=new Map();for(const r of current){const k=identity(r);if(!aggregate.has(k))aggregate.set(k,{article:r.article,size:r.size,location:r.location,pallet:r.pallet,states:Object.fromEntries(CONFIG.states.map(s=>[s,0]))});aggregate.get(k).states[r.state]=(aggregate.get(k).states[r.state]||0)+r.quantity}const original=new Map();for(const r of sourceRows){if(r.sourceRow==null)continue;const k=identity(r);if(!original.has(k))original.set(k,[]);if(!original.get(k).includes(r.sourceRow))original.get(k).push(r.sourceRow)}const seen=new Set();for(const [k,rows] of original){const value=aggregate.get(k),identityRow=value||sourceRows.find(x=>identity(x)===k)||{};rows.forEach((rowIndex,idx)=>{for(const state of CONFIG.states){const c=cols[state];if(c>=0)setCell(ws,rowIndex,c,idx===0?Number(value?.states?.[state]||0):0,ws[X().utils.encode_cell({r:templateRow,c})])}if(idx===0)this.#applyCheckMeta(ws,rowIndex,cols,identityRow,templateRow,checks)});seen.add(k)}let rowIndex=X().utils.decode_range(ws['!ref']||'A1').e.r+1;for(const [k,value] of aggregate){if(seen.has(k))continue;this.#writeIdentityRow(ws,rowIndex,cols,value,templateRow);for(const state of CONFIG.states){const c=cols[state];if(c>=0)setCell(ws,rowIndex,c,Number(value.states[state]||0),ws[X().utils.encode_cell({r:templateRow,c})])}this.#applyCheckMeta(ws,rowIndex,cols,value,templateRow,checks);rowIndex++}extendRef(ws,Math.max(meta.headerRow,rowIndex-1),maxCol)}else{const currentMap=new Map();for(const r of current){const k=fullIdentity(r);currentMap.set(k,(currentMap.get(k)||0)+r.quantity)}const original=new Map();for(const r of sourceRows){if(r.sourceRow==null)continue;const k=fullIdentity(r);if(!original.has(k))original.set(k,[]);if(!original.get(k).includes(r.sourceRow))original.get(k).push(r.sourceRow)}const seen=new Set();for(const [k,rows] of original){const q=currentMap.get(k)||0,identityRow=current.find(x=>fullIdentity(x)===k)||sourceRows.find(x=>fullIdentity(x)===k)||{};rows.forEach((rowIndex,idx)=>{setCell(ws,rowIndex,cols.quantity,idx===0?q:0,ws[X().utils.encode_cell({r:templateRow,c:cols.quantity})]);if(idx===0)this.#applyCheckMeta(ws,rowIndex,cols,identityRow,templateRow,checks)});seen.add(k)}let rowIndex=X().utils.decode_range(ws['!ref']||'A1').e.r+1;for(const r of current){const k=fullIdentity(r);if(seen.has(k))continue;this.#writeIdentityRow(ws,rowIndex,cols,r,templateRow);setCell(ws,rowIndex,cols.state,r.state,ws[X().utils.encode_cell({r:templateRow,c:cols.state})]);setCell(ws,rowIndex,cols.quantity,r.quantity,ws[X().utils.encode_cell({r:templateRow,c:cols.quantity})]);this.#applyCheckMeta(ws,rowIndex,cols,r,templateRow,checks);seen.add(k);rowIndex++}extendRef(ws,Math.max(meta.headerRow,rowIndex-1),maxCol)}\n  }\n  #writeIdentityRow`,'Master control metadata projection');

/* V19 table creation is ZIP-level only: no whole-workbook ExcelJS reserialization. */
mustRegex(/async function patchExcelTableXml\(buffer\)\{[\s\S]*?\nlet xlsxPromise=null;/,`${excelTables}\nlet xlsxPromise=null;`,'Safe V19 OOXML table builder');

html=html.split('EXPORT EXCEL V18').join('EXPORT EXCEL V19');
html=html.split('ricostruisce il Master come vera Tabella Excel nello stesso stile del file di riferimento, con filtri visibili, range aggiornato e righe del Master preservate.').join('esporta un workbook integro con tabelle Excel reali e filtri su tutti i fogli operativi, senza riserializzare i worksheet con ExcelJS.');
fs.writeFileSync('index.html',html,'utf8');
console.log(`NOVA V19 WMS consolidation generated: ${Buffer.byteLength(html)} bytes`);
